export default class loginValidations {
    constructor(username,password) {
        this.email = email,
        this.username = username
    }

    checkValidations() {
        let errors = []
        
    }
}